package MusicLab.StepDefinitions.Chat;

public class GetChatStepDef {
    public GetChatStepDef() {
        Given("^Get chat contents with valid path$", () -> {
        });
        When("^Send request get chat message$", () -> {
        });
        Then("^Status code should be (\\d+) OK$", (Integer arg0) -> {
        });
        Given("^Get chat contents with invalid path$", () -> {
        });
    }
}
